// The admin surface is currently composed entirely of analytics endpoints,
// whose business logic lives in the analytics module
// (modules/analytics/analytics.service.ts). This file is reserved for future
// admin-only operations that don't belong to another module.

export {};
